-- Telescope picker mappings were replaced by config.pickers backed by snacks.nvim.
-- Keep this file as a tombstone so older references to lua/plugins/telescope.lua
-- resolve to an intentionally empty lazy.nvim spec.
return {}
